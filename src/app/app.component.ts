import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html'
})

export class AppComponent {
  //On récupère le code ci-dessous pour éviter les erreurs et l'insérer dans le fichier list-pokemon.ts et supprimer le reste car nous n'en avons plus besoin car au niveau du template, app.component.html, il n'y a plus rien.
  // pokemonList: Pokemon[] = POKEMONS;
}
